🧩 DevOps To-Do List Application

A simple To-Do List web application built with Python (Flask), MySQL, and Docker to demonstrate core DevOps concepts like containerization, environment configuration, and service orchestration using Docker Compose.

🚀 Features

➕ Add new tasks

✏️ Edit existing tasks

🗑️ Delete tasks

✅ Mark tasks as completed

💾 Persistent data storage using MySQL

🐳 Fully containerized with Docker & Docker Compose

🛠️ Tech Stack
Component	Technology Used
Backend	Python (Flask)
Database	MySQL
Containerization	Docker, Docker Compose
Frontend	HTML, CSS (Flask templates)
🧱 Project Structure
📦 todo-flask-app
├── app.py                 # Main Flask application
├── requirements.txt       # Python dependencies
├── templates/             # HTML templates
│   ├── index.html
├── Dockerfile             # Flask app Docker configuration
├── docker-compose.yml     # Multi-container orchestration
└── README.md              # Project documentation

⚙️ Setup & Run Locally (Without Docker)
# 1️⃣ Clone the repository
git clone https://github.com/yourusername/todo-flask-app.git
cd todo-flask-app

# 2️⃣ Create a virtual environment
python -m venv venv
source venv/bin/activate   # (Windows: venv\Scripts\activate)

# 3️⃣ Install dependencies
pip install -r requirements.txt

# 4️⃣ Start MySQL and update DB credentials in app.py if needed

# 5️⃣ Run the app
python app.py


App will be available at: http://localhost:5000

🐳 Run with Docker (Recommended)
1️⃣ Build and Start Containers
docker-compose up --build


This will:

Build the Flask app image

Start a MySQL container

Create a network for communication

Automatically connect Flask ↔ MySQL

2️⃣ Access the Application

Visit http://localhost:5000

🧩 Environment Variables (Optional)

You can modify values in .env or directly in docker-compose.yml:

MYSQL_ROOT_PASSWORD=yourpassword
MYSQL_DATABASE=todo_db
MYSQL_USER=todo_user
MYSQL_PASSWORD=todo_pass

🧰 Example Docker Compose Services
version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: todo_db

🧪 Future Improvements

Add user authentication

Include task due dates and priority levels

Deploy to a cloud platform (AWS / GCP / Render)

Add CI/CD pipeline via GitHub Actions

🧑‍💻 Author

[Your Name]
📧 [your.email@example.com
]
🔗 [LinkedIn Profile] | [GitHub Profile]
